{
	"name": "saedgewell-portfolio",
	"version": "0.1.0",
	"private": true,
	"workspaces": ["apps/*", "packages/*"],
	"scripts": {
		"dev": "bun run --cwd apps/web dev",
		"build": "bun run --cwd apps/web build",
		"start": "bun run --cwd apps/web start",
		"lint": "bun run --cwd apps/web lint",
		"format": "bun run --cwd apps/web format",
		"clean": "bun run --cwd apps/web clean",
		"db:types": "bun run --cwd apps/web db:types"
	},
	"devDependencies": {
		"@biomejs/biome": "latest",
		"typescript": "latest"
	},
	"engines": {
		"node": ">=20.0.0",
		"bun": ">=1.0.0"
	},
	"dependencies": {
		"@octokit/rest": "^21.1.1"
	}
}
